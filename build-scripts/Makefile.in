###############################
# included makefile					  #
# contains some logic targets #
###############################

Version: FORCE
	./build-scripts/write-Version.sh

# set release date in NEWS file
NEWS: FORCE
	sed -i "1s/yyyy-mm-dd/`date +%F`/" NEWS

# dummy target for forcing a rebuild of existing files
FORCE:

# self-explanatory
remove-dev-files:
	rm -f Makefile
	rm -rf build-tools build-scripts

# self-explanatory
remove-reference:
	rm -rf doc/reference
