stages:
  - test
  - build
  - deploy

before_script:
  - apk update
  - apk add nodejs nodejs-npm git
  - npm install
  - npm install cli/

test:
  stage: test
  tags:
    - alpine
  script:
    npm run-script test

build:
  stage: build
  tags:
    - alpine
  script:
    npm run-script build
