/**
 * A collection of background scripts, for which I don't need any direct link,
 * but which need to be loaded and be able to be loaded by require()
 */
define([ './reset', './tabs', './featuredetect', './box', './online',
    './debug' ], function () {
  return arguments;
});
