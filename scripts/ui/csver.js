/**
 * Interface for writing a proper CSV file.
 * 
 * No validation included
 */
define(function () {
  var CSVer = {
    Interface : {
      toCSV : function () {
      },
    }
  };

  return CSVer;
});
