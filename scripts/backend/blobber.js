/**
 * Interface for reading and writing to a blob
 */
define(function () {
  var Blobber = {
    Interface : {
      toBlob : function () {
      },
      fromBlob : function () {
      }
    }
  };

  return Blobber;
});
